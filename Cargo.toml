[package]
name = "via_testcontainers"
version = "0.1.0"
edition = "2024"

[dependencies]
testcontainers = { version = "0.27", features = ["blocking"] }
tokio = "1.50.0"

[dev-dependencies]
testcontainers = { version = "0.27", features = ["blocking"] }
